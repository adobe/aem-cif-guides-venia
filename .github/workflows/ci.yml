# Mirrors CircleCI workflow "build-and-release" (.circleci/config.yml)
#
# Required GitHub secrets (from CircleCI context "CIF Artifactory Cloud"):
#   ARTIFACTORY_CLOUD_USER, ARTIFACTORY_CLOUD_PASS
#   COMMERCE_ENDPOINT, COMMERCE_INTEGRATION_TOKEN
#   VENIA_ACCOUNT_EMAIL, VENIA_ACCOUNT_PASSWORD
# Optional: CODECOV_TOKEN

name: ci

on:
  pull_request:
  push:
    tags:
      - '**'

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  ARTIFACTORY_CLOUD_USER: ${{ secrets.ARTIFACTORY_CLOUD_USER }}
  ARTIFACTORY_CLOUD_PASS: ${{ secrets.ARTIFACTORY_CLOUD_PASS }}
  COMMERCE_ENDPOINT: ${{ secrets.COMMERCE_ENDPOINT }}
  COMMERCE_INTEGRATION_TOKEN: ${{ secrets.COMMERCE_INTEGRATION_TOKEN }}
  VENIA_ACCOUNT_EMAIL: ${{ secrets.VENIA_ACCOUNT_EMAIL }}
  VENIA_ACCOUNT_PASSWORD: ${{ secrets.VENIA_ACCOUNT_PASSWORD }}
  # setup-node / legacy actions on Node 12 require this at workflow level when needed.
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true

jobs:
  build:
    name: build
    # CircleCI Venia build uses cimg/openjdk:11.0-browsers (default medium runner).
    # Use ubuntu-latest — ubuntu-latest-4-cores needs org larger-runners enabled and can queue forever.
    runs-on: ubuntu-latest
    container:
      image: cimg/openjdk:11.0-browsers
      options: --user circleci
    steps:
      - uses: actions/checkout@v4

      # CircleCI: node/install node-version 12.14.1 (nvm inside cimg image).
      - name: Install Node.js 12.14.1
        run: |
          source "${HOME}/.nvm/nvm.sh"
          nvm install 12.14.1
          nvm use 12.14.1
          echo "$(dirname "$(command -v node)")" >> "${GITHUB_PATH}"
          node -v
          npm -v

      # CircleCI: restore_cache on ~/.m2 BEFORE install_components (fallback key maven-repo-v2-).
      - name: Restore Maven repository cache
        uses: actions/cache/restore@v4
        with:
          path: ~/.m2
          key: maven-repo-v2-${{ runner.os }}-${{ github.ref_name }}-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            maven-repo-v2-${{ runner.os }}-${{ github.ref_name }}-
            maven-repo-v2-${{ runner.os }}-
            maven-repo-v2-

      - name: Update permissions
        run: sudo chown -R "$(whoami)" /usr/local/lib/node_modules || true

      # CircleCI build job does NOT configure ~/.m2/settings.xml or -Partifactory-cloud here.
      # Artifactory settings are only used in it-tests.js (-s .circleci/settings.xml -Partifactory-cloud).

      - name: Install CIF Components
        run: bash .github/ci/install-cif-components.sh

      - name: Build
        run: |
          java -version
          mvn -v
          if [[ "${GITHUB_EVENT_NAME}" == "pull_request" ]]; then
            BRANCH_NAME="${GITHUB_HEAD_REF}"
          elif [[ "${GITHUB_REF}" == refs/heads/* ]]; then
            BRANCH_NAME="${GITHUB_REF_NAME}"
          else
            BRANCH_NAME=""
          fi
          if [[ -n "${BRANCH_NAME}" && "${BRANCH_NAME}" != "main" ]]; then
            mvn -B clean install -PfedDev,classic
          else
            mvn -B clean install -Pclassic
          fi

      - name: Run Unit Tests (Jest)
        working-directory: ui.frontend
        run: npm run ci

      # CircleCI: save_cache on ~/.m2 after build (same key family as restore).
      - name: Save Maven repository cache
        if: ${{ !cancelled() }}
        uses: actions/cache/save@v4
        with:
          path: ~/.m2
          key: maven-repo-v2-${{ runner.os }}-${{ github.ref_name }}-${{ hashFiles('**/pom.xml') }}

      - name: Upload coverage to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: unittests
          fail_ci_if_error: false

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          if-no-files-found: warn
          path: |
            all/target/*.zip
            classic/all/target/*.zip
            dependencies/aem-core-cif-components/examples/bundle/target/*.jar

  integration-test-655:
    name: integration-test-655
    needs: build
    runs-on: ubuntu-latest
    container:
      image: docker-adobe-cif-release.dr-uw2.adobeitc.com/circleci-qp:6.4.6-openjdk11
      credentials:
        username: ${{ secrets.ARTIFACTORY_CLOUD_USER }}
        password: ${{ secrets.ARTIFACTORY_CLOUD_PASS }}
      options: --network host
    services:
      aem:
        image: docker-adobe-cif-release.dr-uw2.adobeitc.com/circleci-aem:6.5.23.0-openjdk11
        credentials:
          username: ${{ secrets.ARTIFACTORY_CLOUD_USER }}
          password: ${{ secrets.ARTIFACTORY_CLOUD_PASS }}
        options: --network host
    env:
      AEM: classic
      TYPE: integration
      CI_QP_PATH: /home/circleci/cq
      QP_SERVER_HOSTNAME: localhost
    steps:
      - uses: actions/checkout@v4
      - name: Fix workspace permissions
        run: sudo chmod -R 777 "${GITHUB_WORKSPACE}" /github /__w/_temp || true
      - run: echo "CI_BUILD_PATH=${GITHUB_WORKSPACE}" >> "${GITHUB_ENV}"
      - uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: .
      - run: bash .github/ci/run-integration-test.sh

  integration-test-660:
    name: integration-test-660
    needs: build
    runs-on: ubuntu-latest
    container:
      image: docker-adobe-cif-release.dr-uw2.adobeitc.com/circleci-qp:6.4.7-openjdk21
      credentials:
        username: ${{ secrets.ARTIFACTORY_CLOUD_USER }}
        password: ${{ secrets.ARTIFACTORY_CLOUD_PASS }}
      options: --network host
    services:
      aem:
        image: docker-adobe-cif-release.dr-uw2.adobeitc.com/circleci-aem-lts:6.6.0-openjdk21
        credentials:
          username: ${{ secrets.ARTIFACTORY_CLOUD_USER }}
          password: ${{ secrets.ARTIFACTORY_CLOUD_PASS }}
        options: --network host
    env:
      AEM: lts
      TYPE: integration
      CI_QP_PATH: /home/circleci/cq
      QP_SERVER_HOSTNAME: localhost
    steps:
      - uses: actions/checkout@v4
      - name: Fix workspace permissions
        run: sudo chmod -R 777 "${GITHUB_WORKSPACE}" /github /__w/_temp || true
      - run: echo "CI_BUILD_PATH=${GITHUB_WORKSPACE}" >> "${GITHUB_ENV}"
      - uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: .
      - run: bash .github/ci/run-integration-test.sh

  integration-test-cloudready-with-addon:
    name: integration-test-cloudready-with-addon
    needs: build
    runs-on: ubuntu-latest
    container:
      image: docker-adobe-cif-release.dr-uw2.adobeitc.com/circleci-qp:6.4.7-openjdk21
      credentials:
        username: ${{ secrets.ARTIFACTORY_CLOUD_USER }}
        password: ${{ secrets.ARTIFACTORY_CLOUD_PASS }}
      options: --network host
    services:
      aem:
        image: docker-adobe-cif-release.dr-uw2.adobeitc.com/circleci-aem-cloudready:25194-openjdk21
        credentials:
          username: ${{ secrets.ARTIFACTORY_CLOUD_USER }}
          password: ${{ secrets.ARTIFACTORY_CLOUD_PASS }}
        options: --network host
    env:
      AEM: cloud
      TYPE: integration
      CI_QP_PATH: /home/circleci/cq
      QP_SERVER_HOSTNAME: localhost
    steps:
      - uses: actions/checkout@v4
      - name: Fix workspace permissions
        run: sudo chmod -R 777 "${GITHUB_WORKSPACE}" /github /__w/_temp || true
      - run: echo "CI_BUILD_PATH=${GITHUB_WORKSPACE}" >> "${GITHUB_ENV}"
      - uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: .
      - run: bash .github/ci/run-integration-test.sh

  selenium-chrome-655:
    name: selenium-chrome-655
    needs: build
    runs-on: ubuntu-latest
    container:
      image: docker-adobe-cif-release.dr-uw2.adobeitc.com/circleci-qp:6.4.6-openjdk11
      credentials:
        username: ${{ secrets.ARTIFACTORY_CLOUD_USER }}
        password: ${{ secrets.ARTIFACTORY_CLOUD_PASS }}
      options: --network host
    services:
      aem:
        image: docker-adobe-cif-release.dr-uw2.adobeitc.com/circleci-aem:6.5.23.0-openjdk11
        credentials:
          username: ${{ secrets.ARTIFACTORY_CLOUD_USER }}
          password: ${{ secrets.ARTIFACTORY_CLOUD_PASS }}
        options: --network host
    env:
      AEM: classic
      TYPE: selenium
      BROWSER: chrome
      CI_QP_PATH: /home/circleci/cq
      QP_SERVER_HOSTNAME: localhost
    steps:
      - uses: actions/checkout@v4
      - name: Fix workspace permissions
        run: sudo chmod -R 777 "${GITHUB_WORKSPACE}" /github /__w/_temp || true
      - run: echo "CI_BUILD_PATH=${GITHUB_WORKSPACE}" >> "${GITHUB_ENV}"
      - uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: .
      - run: bash .github/ci/run-integration-test.sh

  selenium-chrome-660:
    name: selenium-chrome-660
    needs: build
    runs-on: ubuntu-latest
    container:
      image: docker-adobe-cif-release.dr-uw2.adobeitc.com/circleci-qp:6.4.7-openjdk21
      credentials:
        username: ${{ secrets.ARTIFACTORY_CLOUD_USER }}
        password: ${{ secrets.ARTIFACTORY_CLOUD_PASS }}
      options: --network host
    services:
      aem:
        image: docker-adobe-cif-release.dr-uw2.adobeitc.com/circleci-aem-lts:6.6.0-openjdk21
        credentials:
          username: ${{ secrets.ARTIFACTORY_CLOUD_USER }}
          password: ${{ secrets.ARTIFACTORY_CLOUD_PASS }}
        options: --network host
    env:
      AEM: lts
      TYPE: selenium
      BROWSER: chrome
      CI_QP_PATH: /home/circleci/cq
      QP_SERVER_HOSTNAME: localhost
    steps:
      - uses: actions/checkout@v4
      - name: Fix workspace permissions
        run: sudo chmod -R 777 "${GITHUB_WORKSPACE}" /github /__w/_temp || true
      - run: echo "CI_BUILD_PATH=${GITHUB_WORKSPACE}" >> "${GITHUB_ENV}"
      - uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: .
      - run: bash .github/ci/run-integration-test.sh

  selenium-chrome-cloudready-with-addon:
    name: selenium-chrome-cloudready-with-addon
    needs: build
    runs-on: ubuntu-latest
    container:
      image: docker-adobe-cif-release.dr-uw2.adobeitc.com/circleci-qp:6.4.7-openjdk21
      credentials:
        username: ${{ secrets.ARTIFACTORY_CLOUD_USER }}
        password: ${{ secrets.ARTIFACTORY_CLOUD_PASS }}
      options: --network host
    services:
      aem:
        image: docker-adobe-cif-release.dr-uw2.adobeitc.com/circleci-aem-cloudready:25194-openjdk21
        credentials:
          username: ${{ secrets.ARTIFACTORY_CLOUD_USER }}
          password: ${{ secrets.ARTIFACTORY_CLOUD_PASS }}
        options: --network host
    env:
      AEM: cloud
      TYPE: selenium
      BROWSER: chrome
      CI_QP_PATH: /home/circleci/cq
      QP_SERVER_HOSTNAME: localhost
    steps:
      - uses: actions/checkout@v4
      - name: Fix workspace permissions
        run: sudo chmod -R 777 "${GITHUB_WORKSPACE}" /github /__w/_temp || true
      - run: echo "CI_BUILD_PATH=${GITHUB_WORKSPACE}" >> "${GITHUB_ENV}"
      - uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: .
      - run: bash .github/ci/run-integration-test.sh
